name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push-image:
    uses: digitalidf/digitalidf-actions/.github/workflows/build-and-push-image.yaml@vv1.0.0
    with:
      image: redis-operator